# X-Gate-AI 重构关键代码示例

## 一、设计模式应用对比重示

### 1.1 策略模式：上游路由策略

**重构前（ProxyAdapter 内联实现）：**
```java
// ProxyAdapter.java 第 452-474 行
private List<UpstreamProvider> resolveCandidates(ModelChannel channel, String requestedModel) {
    String pinned = StrUtil.blankToDefault(channel.getModelName(), "");
    if (StrUtil.isNotBlank(pinned) && !pinned.equals(requestedModel)) {
        throw new CommonException("该 Key 已限定仅可调用模型: " + pinned);
    }
    boolean poolRouting = StrUtil.isBlank(pinned) && GatewayConstant.MODEL_POOL.equals(requestedModel);
    
    LambdaQueryWrapper<UpstreamProvider> wrapper = new LambdaQueryWrapper<UpstreamProvider>()
            .eq(UpstreamProvider::getEnabled, CommonConstant.ENABLED)
            .orderByAsc(UpstreamProvider::getFailCount)
            .orderByAsc(UpstreamProvider::getId);
    
    if (!poolRouting) {
        wrapper.eq(UpstreamProvider::getModelName, requestedModel);
    }
    
    List<UpstreamProvider> providers = upstreamProviderMapper.selectList(wrapper);
    if (providers.isEmpty()) {
        throw new CommonException(poolRouting ? "池内暂无任何启用的渠道" : "池内暂无启用的渠道提供模型: " + requestedModel);
    }
    return providers;
}
```

**重构后（策略接口 + 实现）：**
```java
// UpstreamStrategy.java - 策略接口
public interface UpstreamStrategy {
    List<UpstreamProvider> selectCandidates(ModelChannel channel, String requestedModel);
}

// PollingFailoverStrategy.java - 轮询故障转移策略实现
@Slf4j
@Component
public class PollingFailoverStrategy implements UpstreamStrategy {
    
    private final IUpstreamProviderDao upstreamProviderDao;

    @Override
    public List<UpstreamProvider> selectCandidates(ModelChannel channel, String requestedModel) {
        String pinnedModel = normalizeBlank(channel.getModelName());
        
        // 规则1: Key 限定模型时校验请求模型是否匹配
        if (isNotBlank(pinnedModel) && !pinnedModel.equals(requestedModel)) {
            throw new BadRequestException(
                String.format("该 Key 已限定仅可调用模型: %s，当前请求: %s", pinnedModel, requestedModel));
        }
        
        // 规则2: 全池路由
        boolean poolRouting = isBlank(pinnedModel) && GatewayConstant.MODEL_POOL.equals(requestedModel);
        
        // 构建查询：启用 + 模型匹配 + 按失败次数升序
        LambdaQueryWrapper<UpstreamProvider> wrapper = new LambdaQueryWrapper<UpstreamProvider>()
                .eq(UpstreamProvider::getEnabled, CommonConstant.ENABLED)
                .orderByAsc(UpstreamProvider::getFailCount)
                .orderByAsc(UpstreamProvider::getId);
        
        if (!poolRouting) {
            wrapper.eq(UpstreamProvider::getModelName, requestedModel);
        }
        
        List<UpstreamProvider> candidates = upstreamProviderDao.selectList(wrapper);
        
        // 检查是否有可用候选
        if (candidates.isEmpty()) {
            String errorMsg = poolRouting
                ? "池内暂无任何启用的渠道，请先在控制台配置并启用上游服务"
                : String.format("池内暂无启用的渠道提供模型: %s", requestedModel);
            throw new BadRequestException(errorMsg);
        }
        
        return candidates;
    }
}
```

**改进点：**
- 解耦路由决策与 HTTP 调用逻辑
- 支持动态切换路由策略（如将来需要加权轮询、最少连接等）
- 便于单元测试（可 Mock 策略接口）
- 单一职责：策略只负责选择候选，不处理 HTTP 调用

---

### 1.2 模板方法模式：请求处理流程

**重构前（chat/chatStream/embeddings 重复代码）：**
```java
// ProxyAdapter.java - 三个方法有大量重复逻辑
public String chat(ModelChannel channel, String rawBody) {
    String requestedModel = parseRequestedModel(rawBody);
    List<UpstreamProvider> candidates = resolveCandidates(channel, requestedModel);
    String lastError = "";
    List<String> chain = new ArrayList<>();
    long totalStart = System.currentTimeMillis();
    
    for (int i = 0; i < candidates.size(); i++) {
        // ... 重复的循环逻辑 ...
    }
    // ... 重复的错误处理 ...
}

public void chatStream(ModelChannel channel, String rawBody, Consumer<byte[]> onChunk) {
    String requestedModel = parseRequestedModel(rawBody);
    List<UpstreamProvider> candidates = resolveCandidates(channel, requestedModel);
    // ... 几乎相同的循环逻辑，只是调用方式不同 ...
}
```

**重构后（GatewayService 统一流程）：**
```java
// GatewayService.java
public String chat(ModelChannel channel, String rawBody) {
    String requestedModel = parseRequestedModel(rawBody);
    List<UpstreamProvider> candidates = upstreamStrategy.selectCandidates(channel, requestedModel);
    
    List<String> chain = new ArrayList<>();
    long totalStart = System.currentTimeMillis();
    String lastError = "";

    for (int i = 0; i < candidates.size(); i++) {
        UpstreamProvider provider = candidates.get(i);
        long start = System.currentTimeMillis();

        try {
            String upstreamJson = proxyAdapter.chat(
                    provider,
                    rebindUpstreamModel(rawBody, provider.getModelName()));

            long latencyMs = System.currentTimeMillis() - start;
            JSONObject usage = recordCallLog(channel, provider, rawBody, upstreamJson,
                    latencyMs, 200, "chat");

            chain.add(gatewayLogger.buildChainEntry(provider, "OK", null));
            logCall("chat", channel, requestedModel, rawBody, false,
                    "SUCCESS", provider, System.currentTimeMillis() - totalStart, usage, chain);

            return upstreamJson;
        } catch (Exception e) {
            lastError = resolveMessage(e);
            chain.add(gatewayLogger.buildChainEntry(provider, "FAIL", lastError));
            bumpFailCount(provider);
        }
    }
    
    // 统一的全链路失败处理
    candidates.forEach(this::bumpFailCount);
    logCall(...);
    throw new BadRequestException("所有上游调用失败: " + lastError);
}
```

**改进点：**
- 消除重复的路由和故障转移逻辑
- 每个方法职责清晰：GatewayService 编排流程，ProxyAdapter 执行 HTTP，Logger 记录日志
- 易于扩展新的请求类型（如补充请求）

---

## 二、异常处理体系重构

**重构前（单一异常类）：**
```java
// CommonException.java
public class CommonException extends RuntimeException {
    public CommonException(String message) {
        super(message);
    }
}

// GlobalExceptionHandler.java
@ExceptionHandler(CommonException.class)
public HttpResponse handleCommonException(CommonException ex) {
    return HttpResponse.error(ex.getMessage());
}
```

**重构后（异常层次结构）：**
```java
// GatewayException.java - 基类
public class GatewayException extends RuntimeException {
    private final int statusCode;
    private final String errorCode;
    
    public GatewayException(String message, int statusCode, String errorCode) {
        super(message);
        this.statusCode = statusCode;
        this.errorCode = errorCode;
    }
}

// AuthenticationException.java - 认证失败
public class AuthenticationException extends GatewayException {
    public AuthenticationException(String message) {
        super(message, 401, "invalid_api_key");
    }
}

// UpstreamException.java - 上游服务异常
public class UpstreamException extends GatewayException {
    private final int upstreamHttpStatus;
    
    public UpstreamException(String message, int upstreamHttpStatus) {
        super(message, 502, "upstream_error");
        this.upstreamHttpStatus = upstreamHttpStatus;
    }
}

// BadRequestException.java - 请求参数异常
public class BadRequestException extends GatewayException {
    public BadRequestException(String message) {
        super(message, 400, "invalid_request_error");
    }
}

// GlobalExceptionHandler.java - 统一处理
@ExceptionHandler(AuthenticationException.class)
public ResponseEntity<HttpResponse> handleAuthenticationException(AuthenticationException ex) {
    log.warn("认证失败: {}", ex.getMessage());
    return ResponseEntity
            .status(ex.getStatusCode())
            .body(HttpResponse.error(ex.getMessage()));
}

@ExceptionHandler(GatewayException.class)
public ResponseEntity<HttpResponse> handleGatewayException(GatewayException ex) {
    log.warn("网关业务异常 [{}]: {}", ex.getErrorCode(), ex.getMessage());
    return ResponseEntity
            .status(ex.getStatusCode())
            .body(HttpResponse.error(ex.getMessage()));
}
```

**改进点：**
- 异常类型明确，便于分类处理
- 携带 HTTP 状态码和错误码，符合 OpenAI 协议规范
- 日志级别更精确（认证失败用 warn，系统异常用 error）

---

## 三、Mapper 层接口提取

**重构前（直接注入 Mapper）：**
```java
// AdminService.java
@Resource
private UpstreamProviderMapper upstreamProviderMapper;

// ProxyAdapter.java
@Resource
private UpstreamProviderMapper upstreamProviderMapper;
```

**重构后（统一 DAO 接口）：**
```java
// IUpstreamProviderDao.java - 接口定义
@Mapper
public interface IUpstreamProviderDao extends BaseMapper<UpstreamProvider> {
}

// PollingFailoverStrategy.java - 通过构造器注入
public class PollingFailoverStrategy implements UpstreamStrategy {
    private final IUpstreamProviderDao upstreamProviderDao;
    
    public PollingFailoverStrategy(IUpstreamProviderDao upstreamProviderDao) {
        this.upstreamProviderDao = upstreamProviderDao;
    }
}

// AdminManagementServiceImpl.java - 通过构造器注入
public class AdminManagementServiceImpl implements AdminManagementService {
    private final IUpstreamProviderDao upstreamProviderDao;
    private final IModelChannelDao modelChannelDao;
    
    public AdminManagementServiceImpl(IUpstreamProviderDao upstreamProviderDao,
                                      IModelChannelDao modelChannelDao,
                                      ProxyAdapter proxyAdapter) {
        this.upstreamProviderDao = upstreamProviderDao;
        this.modelChannelDao = modelChannelDao;
        this.proxyAdapter = proxyAdapter;
    }
}
```

**改进点：**
- 接口隔离，便于 Mock 测试
- 构造器注入替代字段注入，依赖关系更明确
- 符合依赖倒置原则（Depend on abstractions, not concretions）

---

## 四、日志组件职责分离

**重构前（日志逻辑散落在 ProxyAdapter）：**
```java
// ProxyAdapter.java 第 271-363 行 - 约 100 行日志代码
private void logCall(...) { ... }
private String chainEntry(...) { ... }
private String payloadDesc(...) { ... }
private void printBoxedLog(...) { ... }
// ... 以及大量辅助方法
```

**重构后（独立日志组件）：**
```java
// GatewayLogger.java - 单一职责的日志组件
@Slf4j
@Component
public class GatewayLogger {
    
    private static final Logger GATEWAY_LOGGER = LoggerFactory.getLogger("GATEWAY");
    private static final Logger GATEWAY_CONSOLE = LoggerFactory.getLogger("GATEWAY_CONSOLE");
    
    /**
     * 输出网关调用日志（单行格式 + 框式格式）
     */
    public void logCall(String type, ModelChannel channel, String requestedModel,
                        String rawBody, boolean stream, String result,
                        UpstreamProvider finalProvider, long costMs,
                        JSONObject usage, List<String> chain) {
        // 1. 单行日志（写入 gateway.log）
        String singleLineLog = buildSingleLineLog(...);
        GATEWAY_LOGGER.info(singleLineLog);
        
        // 2. 框式日志（仅控制台）
        printBoxedLog(...);
    }
    
    /**
     * 构建故障转移链路条目
     */
    public String buildChainEntry(UpstreamProvider provider, String status, String error) {
        // ...
    }
    
    /**
     * 构建调用日志实体（用于落库）
     */
    public CallLog buildCallLogEntry(ModelChannel channel, UpstreamProvider provider, ...) {
        // ...
    }
}

// GatewayService.java - 使用日志组件
public class GatewayService {
    private final GatewayLogger gatewayLogger;
    
    // 调用日志
    gatewayLogger.logCall(type, channel, requestedModel, rawBody, stream,
            result, finalProvider, costMs, usage, chain);
}
```

**改进点：**
- 日志逻辑集中管理，便于维护和修改格式
- GatewayService 不再关心日志细节
- 可独立测试日志输出

---

## 五、Controller 层简化

**重构前（Controller 包含大量辅助方法）：**
```java
// GatewayController.java 第 116-185 行 - 约 70 行辅助方法
private void initSse(HttpServletResponse response) throws IOException { ... }
private void writeSseError(ServletOutputStream out, String message) { ... }
private void writeBytes(ServletOutputStream out, byte[] bytes) { ... }
private void writeError(HttpServletResponse response, ServletOutputStream out, ...) { ... }
private ResponseEntity<String> buildErrorResponse(int status, String type, String message) { ... }
private String errorBody(String type, String message) { ... }
private ModelChannel resolveChannel(HttpServletRequest request) { ... }
```

**重构后（Controller 只负责路由）：**
```java
// GatewayController.java - 简洁的 Controller
@Slf4j
@RestController
@RequestMapping("/v1")
public class GatewayController {
    
    private final GatewayService gatewayService;

    @PostMapping("/chat/completions")
    public void chatCompletions(@RequestBody String rawBody,
                                HttpServletRequest request,
                                HttpServletResponse response) throws IOException {
        ServletOutputStream out = response.getOutputStream();
        ModelChannel channel = resolveChannel(request);

        if (channel == null) {
            writeError(response, out, 400, "invalid_request_error", "无法识别调用方对客服务");
            return;
        }

        try {
            if (body.getBooleanValue("stream")) {
                initSseResponse(response);
                gatewayService.chatStream(channel, rawBody, chunk -> writeBytes(out, chunk));
            } else {
                String upstreamJson = gatewayService.chat(channel, rawBody);
                response.setContentType(MediaType.APPLICATION_JSON_VALUE);
                out.write(upstreamJson.getBytes(StandardCharsets.UTF_8));
                out.flush();
            }
        } catch (Exception ex) {
            writeError(response, out, 500, "server_error", ex.getMessage());
        }
    }
    
    // 辅助方法精简为必要的几行
    private ModelChannel resolveChannel(HttpServletRequest request) {
        Object attr = request.getAttribute(GatewayConstant.ATTR_API_KEY);
        return attr instanceof ModelChannel ch ? ch : null;
    }
}
```

**改进点：**
- Controller 职责单一：只负责请求路由和响应格式
- 业务逻辑委托给 Service 层
- 代码长度从 186 行减少到约 100 行

---

## 六、关键指标对比

| 维度 | 重构前 | 重构后 | 改进 |
|------|--------|--------|------|
| ProxyAdapter 行数 | 972 行 | 150 行 | -84% |
| GatewayService 行数 | 无 | 200 行 | 新增职责 |
| GatewayLogger 行数 | 散落在 Adapter | 250 行 | 集中管理 |
| Controller 行数 | 186 行 | 100 行 | -46% |
| 异常类数量 | 1 个 | 5 个 | 层次化 |
| Mapper 接口 | 无 | 3 个 | 可测试性 |
| 设计模式应用 | 无 | 策略模式 | 可扩展性 |

---

## 七、重构后的包结构

```
com.xgateai
├── controller/
│   ├── GatewayController.java      # 对外网关控制器
│   └── AdminController.java        # 管理端控制器
├── service/
│   ├── gateway/
│   │   ├── GatewayService.java     # 网关核心服务
│   │   ├── strategy/
│   │   │   ├── UpstreamStrategy.java    # 策略接口
│   │   │   └── PollingFailoverStrategy.java # 轮询故障转移策略
│   │   └── adapter/
│   │       └── ProxyAdapter.java      # HTTP 透传适配器
│   ├── admin/
│   │   ├── AdminManagementService.java    # 管理接口
│   │   └── AdminManagementServiceImpl.java # 管理实现
│   └── log/
│       ├── CallLogAnalysisService.java    # 日志分析接口
│       └── CallLogAnalysisServiceImpl.java # 日志分析实现
├── logging/
│   ├── GatewayLogger.java      # 网关日志组件
│   ├── GatewayLog.java         # MDC 工具
│   ├── TraceFilter.java        # 请求链路过滤器
│   └── TraceIdConverter.java   # Logback 转换器
├── interceptor/
│   └── ApiKeyInterceptor.java  # API Key 鉴权拦截器
├── config/
│   ├── WebMvcConfig.java       # Web MVC 配置
│   ├── MyBatisPlusConfig.java  # MyBatis-Plus 配置
│   ├── OkHttpConfig.java       # OkHttp 客户端配置
│   └── GatewayConfig.java      # 网关配置属性
├── mapper/
│   ├── IUpstreamProviderDao.java
│   ├── IModelChannelDao.java
│   └── ICallLogDao.java
├── entity/
│   ├── UpstreamProvider.java
│   ├── ModelChannel.java
│   └── CallLog.java
├── dto/
│   ├── ProviderDTO.java
│   ├── ChannelDTO.java
│   └── LogQueryDTO.java
├── response/
│   └── HttpResponse.java
├── exception/
│   ├── GatewayException.java
│   ├── AuthenticationException.java
│   ├── UpstreamException.java
│   ├── ResourceNotFoundException.java
│   ├── BadRequestException.java
│   └── GlobalExceptionHandler.java
└── component/
    └── EncryptUtil.java
```
