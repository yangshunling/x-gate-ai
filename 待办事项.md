=============================================================================
完整工程代码精细审查与优化任务提示词（后端 + 前端）
=============================================================================

本提示词分为【Part A - 后端（Java）】和【Part B - 前端（视具体技术栈）】两个独立模块。
请根据实际项目情况，选择对应的模块执行。


================================================================================
PART A - 后端（Java）精细审查
================================================================================

一、任务定位
-----------------------------------------------
针对已初步重构的 Java 后端工程代码（Spring Boot 3.4.4 + MyBatis-Plus + HuTool + FastJson），
进行类级/函数级/变量级的精细化审查与优化。
目标：使代码达到工业级交付标准 —— 逻辑简洁、充分利用现有依赖、Javadoc 完备、异常健壮。


二、可用依赖清单（严格限定，禁止引入新依赖）
-----------------------------------------------
以下为当前工程（pom.xml）已引入的依赖，优化时优先使用：

【Spring Boot 3.4.4】
- spring-boot-starter-web → RestTemplate、统一异常处理（@ControllerAdvice）、拦截器
- spring-boot-starter-validation → @Valid / @NotNull / @NotBlank / @Size / @Pattern

【MyBatis-Plus 3.5.11】
- ServiceImpl<T, M> / IService<T> → 通用 CRUD
- LambdaQueryWrapper / LambdaUpdateWrapper → 类型安全的条件构造
- Page<T> → 分页查询
- @TableLogic / @Version / @TableField / @TableId

【HuTool 5.8.47】（重点复用）
- StrUtil → isBlank() / isNotBlank() / format() / sub() / split() / trim()
- CollUtil → isEmpty() / isNotEmpty() / newArrayList() / newHashSet() / groupBy() / join()
- BeanUtil → copyProperties() / toBean() / beanToMap()
- DateUtil → parse() / format() / between() / offset() / now()
- IdUtil → getSnowflakeNextId() / fastUUID() / getNanoId()
- ObjectUtil → isNull() / isNotNull() / defaultIfNull()
- EnumUtil → getBy() / like()
- FileUtil / IoUtil → 文件读写
- HttpUtil → HTTP 请求（可选，也可用 OkHttp）

【FastJson 2.0.64】
- JSON.toJSONString() / JSON.parseObject() / JSON.parseArray()
- JSONObject / JSONArray 操作
- 统一 JSON 序列化/反序列化，禁止手写 JSON 拼接

【OkHttp 4.12.0】
- 用于出站 HTTP/SSE 调用，配合拦截器统一日志与重试

【Lombok 1.18.46】
- @Data / @Builder / @Slf4j / @AllArgsConstructor / @NoArgsConstructor / @RequiredArgsConstructor
- @Accessors(chain = true) 链式调用

【SQLite JDBC】
- 通过 MyBatis-Plus 驱动，不直接使用原始 JDBC API


三、精细化优化目标
-----------------------------------------------

3.1 函数级优化（核心）

A. 逻辑简化
- 函数体超过 30 行必须拆分，每个函数只做一件事（单一职责）
- 嵌套深度不超过 2 层（if/for/while），使用早返回（early return）
- 复杂条件判断提取为独立方法，或使用枚举 + 策略模式
- 使用 Stream API 简化集合过滤/转换/分组
- 使用 Optional 替代多层 null 判读

B. 框架工具替换（遇到以下场景，强制使用对应依赖）
- 字符串判空/拼接/截取 → StrUtil
- 集合判空/新建/分组/连接 → CollUtil
- 对象属性复制/转换 → BeanUtil
- 日期解析/格式化/计算 → DateUtil
- 生成唯一 ID → IdUtil（雪花 ID 或 UUID）
- JSON 序列化/反序列化 → JSON（FastJson）
- 参数校验 → @NotBlank / @NotNull（javax.validation）
- 对象判空 → ObjectUtil
- 日志记录 → @Slf4j + 占位符（禁止字符串拼接）
- 枚举操作 → EnumUtil
- 文件读写 → FileUtil / IoUtil

原则：凡 3 行以上手写代码可替换为 1 行框架调用的，立即替换。

C. 异常与边界处理
- 每个 public 方法明确异常声明或抛出自定义业务异常
- 所有外部输入（参数、请求体、配置、文件、网络响应）必须做非空/合法性校验
- 集合/数组操作前检查 size，避免越界
- 资源操作（IO、连接）使用 try-with-resources 或确保 finally 释放

3.2 类级优化
- 单一职责检查：职责过重的类拆分为多个小类
- 面向接口编程：定义接口与实现分离
- 消除“万能类”（CommonUtils / Helper），按功能拆分专用工具类
- 常量集中管理：使用常量类或枚举，禁止魔法值

3.3 变量级优化
- 命名语义明确：startTime > t1，customerIdList > list
- 作用域最小化：能放在循环内/方法内的不提升到类级别
- 消除无用临时变量：直接 return calculate();
- 正确使用 final 修饰不可变变量
- 类型合理选型：金额→BigDecimal，状态码→Enum，ID→Long/String

3.4 Javadoc 注释规范（强制执行）
每个类、每个 public/protected 方法、每个 public 常量/字段，都必须有完整 Javadoc。

类注释模板：
/**
* [Brief description of the class's responsibility].
*
* <p>[Detailed description: core functionality, usage scenarios,
* thread-safety, important notes]</p>
*
* @author [Developer Name]
* @since [Version]
  */

方法注释模板：
/**
* [Brief description of the method].
*
* @param paramName [description, range, nullable or not]
* @param paramName2 [description]
* @return [description, possible values]
* @throws [ExceptionType] [condition that triggers this exception]
* @see [Related method/class]
  */

字段注释模板：
/**
* [Field meaning, including unit, format, default value if applicable].
  */

行内注释要求：复杂逻辑说明"为什么这么做"，禁止无意义注释。

四、审查顺序与输出规范
-----------------------------------------------

【建议审查顺序】
1. Entity / DTO / VO → 统一字段命名、类型、映射、Javadoc
2. Enum / Constants → 检查复用性、补充注释
3. Mapper（DAO） → 检查 SQL/MyBatis-Plus 查询规范
4. Service 实现类（核心）→ 重点拆分长方法、引入框架工具
5. Controller → 统一参数校验、统一响应封装、全局异常处理


================================================================================
PART B - 前端（JavaScript/TypeScript）精细审查
================================================================================

一、前端定位说明
-----------------------------------------------
请根据实际前端技术栈选择对应的优化指南：
- [ ] Vue 2 / Vue 3
- [ ] React
- [ ] Angular
- [ ] 原生 HTML + CSS + JavaScript
- [ ] Thymeleaf（服务端模板）
- [ ] 其他（请注明）：___________

以下为通用前端优化规范，适用于所有前端技术栈。


二、前端通用优化目标
-----------------------------------------------

2.1 文件与目录结构优化
- 按功能/模块划分目录（components / views / api / utils / store / assets）
- 公共组件放入 common/ 或 shared/ 目录
- API 请求统一封装，按业务模块拆分文件
- 工具函数（utils/）按功能拆分：format.ts / validate.ts / storage.ts / http.ts

2.2 组件级优化
- 组件职责单一，单个组件不超过 300 行（不含模板）
- 抽取重复 UI 为公共组件
- 抽取重复逻辑为 Composables（Vue）或 Hooks（React）或 Mixins（Angular）
- 大组件拆分为子组件树，通过 props/events 传递数据
- 避免深层次 props 传递（使用 provide/inject 或状态管理）

2.3 函数级优化
- 函数不超过 30 行，单一职责
- 使用早返回减少嵌套
- 复杂条件判断抽取为独立函数
- 异步操作统一使用 async/await，错误处理完整（try-catch）
- 使用数组高阶函数（map / filter / reduce / some / every）替代 for 循环

2.4 API 请求优化
- 统一封装请求实例（axios / fetch），配置 baseURL、超时、拦截器
- 统一错误处理（401/403/500 等状态码全局处理）
- API 响应统一解构（如 const { code, data, msg } = response）
- 接口类型定义完整（TypeScript 项目必须）
- 请求/响应拦截器中统一添加 Token、日志

2.5 状态管理优化（如使用 Pinia / Vuex / Redux / Zustand）
- 按模块拆分 store
- 避免在组件中直接修改 state，通过 actions/mutations 修改
- 异步操作放在 actions 中，使用 async/await
- 合理使用 getters / computed 派生状态

2.6 样式优化
- 使用 CSS 变量或主题变量统一管理颜色/字体/间距
- 避免 !important，按权重层叠
- 抽取公共样式到全局样式文件
- 使用 CSS Modules / Scoped / Tailwind（按项目实际）
- 响应式设计，移动端适配

2.7 性能优化
- 列表渲染使用唯一 key
- 大列表使用虚拟滚动
- 图片懒加载
- 组件懒加载（路由懒加载 / 异步组件）
- 防抖/节流处理高频事件（搜索输入、滚动、resize）
- 避免在模板中使用复杂计算表达式

2.8 注释规范（强制执行）
- 每个组件顶部添加组件说明：功能、使用场景、props 说明
- 每个复杂函数添加 JSDoc 注释（支持 IDE 智能提示）
- API 接口函数必须添加注释：说明接口用途、参数、返回值

JSDoc 模板（JavaScript/TypeScript）：
/**
* [函数简短描述]
* @param {类型} paramName - [参数说明]
* @param {类型} paramName2 - [参数说明]
* @returns {类型} [返回值说明]
* @example
* // 使用示例
* const result = functionName('test', 123);
  */
  组件注释模板（Vue SFC 顶部）：
  <!--
   * Component: UserProfile
   * Description: 展示用户个人信息卡片，支持编辑和保存
   * Props:
   *   - userId (String, required): 用户ID
   *   - editable (Boolean, default: false): 是否可编辑
   * Events:
   *   - save: 保存成功后触发
   *   - cancel: 取消编辑时触发
  -->


三、前端审查顺序与输出规范
-----------------------------------------------

【建议审查顺序】
1. API 请求封装层（http / axios 实例、拦截器）
2. 工具函数（utils/）：format / validate / storage
3. 状态管理（store/）：按模块审查
4. 公共组件（components/common/）
5. 业务组件（views/ 或 pages/）：按路由模块逐个审查
6. 样式文件（全局样式、主题变量）
7. 路由配置（router/）：懒加载、权限控制

【前端输出物清单】
1. 逐文件审查报告（Markdown 格式）
2. 优化后完整前端代码（按原目录结构输出）
3. 变更对照表
4. 组件依赖关系图（可选）


四、前端约束条件
-----------------------------------------------
1. 不改变页面交互逻辑和 UI 视觉效果
2. 不修改后端 API 接口定义
3. 不引入新的第三方库（除非必要且有充分理由）
4. 每次修改一个文件/组件，确保功能正常后再继续
5. TypeScript 项目必须保持类型完整（禁止使用 any 除非必要）


五、前端问题描述示例
-----------------------------------------------
文件：src/views/OrderList.vue
问题1：组件共 487 行，包含搜索、列表、分页、弹窗编辑等多种逻辑
优化方案：拆分为 OrderSearchBar.vue + OrderTable.vue + OrderPagination.vue + OrderEditModal.vue
问题2：API 请求直接使用 axios，未统一封装，重复配置 baseURL 和 Token
优化方案：创建 src/api/request.ts 统一封装，并拆分订单 API 到 src/api/order.ts
问题3：搜索框输入触发接口请求，未做防抖
优化方案：使用 lodash.debounce 或手动实现防抖，延迟 300ms
问题4：组件无注释，props 含义不明确
优化方案：添加组件顶部注释和 props 类型说明


=============================================================================
七、AI 执行流程（请按此步骤执行）
=============================================================================
Step 1：确认项目结构（后端 + 前端分别是什么技术栈）
Step 2：后端从 entity 包开始，逐个审查
Step 3：前端从 api 封装层开始，逐个模块审查
Step 4：每完成一个包/模块，输出阶段性报告
Step 5：全部完成后输出最终汇总报告